---
// MachuPicchu.astro - Hero Section Premium
const tours = [
  {
    image: "../../public/Camino-Inca-Express.webp",
    title: "Inca Trail Express",
    subtitle: "KM 104 + Intipunku + Machu Picchu",
    duration: "Full Day",
    price: "US$ 380.00",
    difficulty: "Easy",
    rating: 4.9,
    featured: true,
    slug: "https://wa.link/obdnm6",
    detailsSlugs: "https://sakuraexpeditions.com/inca-trail-express-full-day/"
  },
  {
    image: "../../public/Camino-Inca-2-Dias.webp",
    title: "Inca Trail 2 days",
    subtitle: "Original Route to Machu Picchu",
    duration: "4 Days / 3 Nights",
    price: "US$ 465.00",
    difficulty: "Moderate",
    rating: 4.8,
    featured: true,
    slug: "https://sakuraexpeditions.com/booking/?id=38",
    detailsSlugs: "https://sakuraexpeditions.com/2-days-short-inca-trail/"
  },
  {
    image: "../../public/Camino-Inca-4-Dias.webp",
    title: "Inca Trail 4 Days",
    subtitle: "The Best Complete Experience",
    duration: "7 Days / 6 Nights",
    price: "US$ 690.00",
    difficulty: "Moderate",
    rating: 5.0,
    featured: true,
    slug: "https://sakuraexpeditions.com/booking/?id=31",
    detailsSlugs: "https://sakuraexpeditions.com/inca-trail-to-machu-picchu-4-days/"
  }
];

const socialLinks = [
  { 
    name: 'Facebook', 
    icon: 'facebook', 
    url: 'https://www.facebook.com/Sakura.Expedition',
    color: '#1877f2'
  },
  { 
    name: 'Instagram', 
    icon: 'instagram', 
    url: 'https://www.instagram.com/sakura.expedition?igsh=ZXBsMDhrajY1ZXgz',
    color: '#e4405f'
  },
  { 
    name: 'TikTok', 
    icon: 'tiktok', 
    url: 'https://www.tiktok.com/@sakuraexpedition?is_from_webapp=1&sender_device=pc',
    color: '#000000'
  },
  { 
    name: 'TripAdvisor', 
    icon: 'tripadvisor', 
    url: 'https://tripadvisor.com',
    color: '#00af87'
  },
  { 
    name: 'YouTube', 
    icon: 'youtube', 
    url: 'https://www.youtube.com/@SakuraExpedition',
    color: '#ff0000'
  }
];
---

<section class="hero-section">
  <!-- Overlay con patrón -->
  <div class="hero-overlay"></div>
  <div class="hero-pattern"></div>
  
  <!-- Decorative elements -->
  <div class="floating-element element-1"></div>
  <div class="floating-element element-2"></div>
  <div class="floating-element element-3"></div>

  <div class="hero-container">
    <!-- Social Media Sidebar - Fijo en desktop -->
    <aside class="social-sidebar">
      <div class="social-line"></div>
      {socialLinks.map((social) => (
        <a 
          href={social.url} 
          aria-label={social.name}
          class="social-link"
          data-social={social.icon}
          target="_blank"
          rel="noopener noreferrer"
        >
          {social.icon === 'facebook' && (
            <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor">
              <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
            </svg>
          )}
          {social.icon === 'instagram' && (
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
              <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
              <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
            </svg>
          )}
          {social.icon === 'tiktok' && (
            <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor">
              <path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-5.2 1.74 2.89 2.89 0 0 1 2.31-4.64 2.93 2.93 0 0 1 .88.13V9.4a6.84 6.84 0 0 0-1-.05A6.33 6.33 0 0 0 5 20.1a6.34 6.34 0 0 0 10.86-4.43v-7a8.16 8.16 0 0 0 4.77 1.52v-3.4a4.85 4.85 0 0 1-1-.1z"/>
            </svg>
          )}
          {social.icon === 'tripadvisor' && (
            <svg width="22" height="22" viewBox="0 0 512 320" fill="currentColor">
              <path class="st0" d="M128.2 127.9C92.7 127.9 64 156.6 64 192c0 35.4 28.7 64.1 64.1 64.1 35.4 0 64.1-28.7 64.1-64.1.1-35.4-28.6-64.1-64-64.1zm0 110c-25.3 0-45.9-20.5-45.9-45.9s20.5-45.9 45.9-45.9S174 166.7 174 192s-20.5 45.9-45.8 45.9z"/>
              <circle class="st0" cx="128.4" cy="191.9" r="31.9"/>
              <path class="st0" d="M384.2 127.9c-35.4 0-64.1 28.7-64.1 64.1 0 35.4 28.7 64.1 64.1 64.1 35.4 0 64.1-28.7 64.1-64.1 0-35.4-28.7-64.1-64.1-64.1zm0 110c-25.3 0-45.9-20.5-45.9-45.9s20.5-45.9 45.9-45.9S430 166.7 430 192s-20.5 45.9-45.8 45.9z"/>
              <circle class="st0" cx="384.4" cy="191.9" r="31.9"/>
              <path class="st0" d="M474.4 101.2l37.7-37.4h-76.4C392.9 29 321.8 0 255.9 0c-66 0-136.5 29-179.3 63.8H0l37.7 37.4C14.4 124.4 0 156.5 0 192c0 70.8 57.4 128.2 128.2 128.2 32.5 0 62.2-12.1 84.8-32.1l43.4 31.9 42.9-31.2-.5-1.2c22.7 20.2 52.5 32.5 85.3 32.5 70.8 0 128.2-57.4 128.2-128.2-.1-35.4-14.6-67.5-37.9-90.7zM368 64.8c-60.7 7.6-108.3 57.6-111.9 119.5-3.7-62-51.4-112.1-112.3-119.5 30.6-22 69.6-32.8 112.1-32.8S337.4 42.8 368 64.8zM128.2 288.2C75 288.2 32 245.1 32 192s43.1-96.2 96.2-96.2 96.2 43.1 96.2 96.2c-.1 53.1-43.1 96.2-96.2 96.2zm256 0c-53.1 0-96.2-43.1-96.2-96.2s43.1-96.2 96.2-96.2 96.2 43.1 96.2 96.2c-.1 53.1-43.1 96.2-96.2 96.2z"/>
            </svg>
          )}
          {social.icon === 'youtube' && (
            <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor">
              <path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/>
            </svg>
          )}
          <span class="social-tooltip">{social.name}</span>
        </a>
      ))}
      <div class="social-line"></div>
    </aside>

    <!-- Main Content -->
    <div class="main-content">
      <!-- Announcement Banner -->
      <div class="announcement-banner">
        <div class="announcement-content">
          <div class="announcement-icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"></circle>
              <path d="M12 6v6l4 2"></path>
            </svg>
          </div>
          
          <div class="announcement-text">
            <span class="urgent-badge">LAST MINUTE!</span>
            <span class="message">
              <strong>LIMITED AVAILABILITY</strong> - Only
              <span class="spots-remaining">
                <span class="number">12</span> 
                <span class="spots-text">spots left</span>
              </span>
              for 2026 Inca Trail
            </span>
          </div>

          <div class="announcement-actions">
            <a href="/booking" class="reserve-button">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M5 12h14M12 5l7 7-7 7"/>
              </svg>
              <span>Secure Your Spot</span>
            </a>
          </div>

          <button class="close-banner" aria-label="Close announcement">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <line x1="18" y1="6" x2="6" y2="18"></line>
              <line x1="6" y1="6" x2="18" y2="18"></line>
            </svg>
          </button>
        </div>

        <div class="progress-bar"></div>
      </div>

      <!-- Badge superior -->
      <div class="top-badge">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="10"></circle>
          <polyline points="12 6 12 12 16 14"></polyline>
        </svg>
        <span>2026 Season - Early Bird</span>
      </div>
      

      <!-- Text Content -->
      <div class="text-content">
        <h1>
          <span class="title-line">Inca Trail and </span>
          <span class="title-line highlight">Machu Picchu 2026</span>
        </h1>
        <p class="hero-description">
          Embark on the adventure of a lifetime along the ancient Inca trails.
          Discover breathtaking landscapes, mystical ruins, and the majesty of Machu Picchu.
          Our guided tours will take you on an unforgettable experience.
        </p>
        
        <!-- Stats -->
        <div class="stats-row">
          <div class="stat-item">
            <span class="stat-number">2500+</span>
            <span class="stat-label">Happy Travelers</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">10+</span>
            <span class="stat-label">Years of Experience</span>
          </div>
          <div class="stat-item">
            <span class="stat-number">4.8★</span>
            <span class="stat-label">Average Rating</span>
          </div>
        </div>
      </div>

      <!-- Tour Cards -->
      <div class="tour-cards">
        {tours.map((tour) => (
          <article class="tour-card" data-featured={tour.featured}>
            {tour.featured && (
              <div class="featured-badge">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor">
                  <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
                </svg>
                Best seller
              </div>
            )}
            
            <div class="card-image-wrapper">
              <img src={tour.image} alt={tour.title} loading="lazy" />
              <div class="image-gradient"></div>
              
              <!-- Badges sobre la imagen -->
              <div class="image-badges">
                <span class="difficulty-badge" data-difficulty={tour.difficulty.toLowerCase()}>
                  {tour.difficulty}
                </span>
                <span class="rating-badge">
                  <svg width="12" height="12" viewBox="0 0 24 24" fill="currentColor">
                    <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
                  </svg>
                  {tour.rating}
                </span>
              </div>
            </div>

            <div class="card-content">
              <h3 class="card-title">{tour.title}</h3>
              <p class="card-subtitle">{tour.subtitle}</p>
              
              <div class="card-info">
                <div class="info-item">
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                    <line x1="16" y1="2" x2="16" y2="6"></line>
                    <line x1="8" y1="2" x2="8" y2="6"></line>
                    <line x1="3" y1="10" x2="21" y2="10"></line>
                  </svg>
                  <span>{tour.duration}</span>
                </div>
                
                <div class="price-tag">
                  <span class="from-label">From</span>
                  <span class="price-value">{tour.price}</span>
                </div>
              </div>

              <a href={tour.slug} class="card-button">
                <span>book now</span>
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <path d="M5 12h14M12 5l7 7-7 7"/>
                </svg>
              </a>

              <a href={tour.detailsSlugs} class="card-button-details" target="_blank" rel="noopener noreferrer">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                  <circle cx="10" cy="10" r="7"/>
                  <line x1="21" y1="21" x2="15" y2="15"/>
                </svg>
                <span>See Details</span>
              </a>
            </div>

            <!-- Hover shine effect -->
            <div class="card-shine"></div>
          </article>
        ))}
      </div>

      <!-- Call to Action -->
      <div class="cta-section">
        <div class="cta-buttons">
          <a href="/tours" class="cta-button">
            <span>Check availability</span>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
          <a href="/contact" class="cta-button">
            <span>Inquire now</span>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7"/>
            </svg>
          </a>
        </div>

        <p class="cta-note">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
            <polyline points="22 4 12 14.01 9 11.01"></polyline>
          </svg>
          Book with confidence - Flexible cancellation
        </p>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="scroll-indicator">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</section>

<script>
  // Funcionalidad para cerrar el banner de anuncio
  document.addEventListener('DOMContentLoaded', () => {
    const closeButton = document.querySelector('.close-banner');
    const banner = document.querySelector('.announcement-banner');

    if (closeButton && banner) {
      closeButton.addEventListener('click', () => {
        banner.style.animation = 'slideUp 0.4s ease-out forwards';
        
        setTimeout(() => {
          banner.style.display = 'none';
        }, 400);
      });
    }
  });

  // Agregar animación de slideUp
  const style = document.createElement('style');
  style.textContent = `
    @keyframes slideUp {
      from {
        transform: translateY(0);
        opacity: 1;
        max-height: 200px;
      }
      to {
        transform: translateY(-20px);
        opacity: 0;
        max-height: 0;
      }
    }
  `;
  document.head.appendChild(style);
</script>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  .hero-section {
    position: relative;
    min-height: 100vh;
    background-image: url('/inti-punku-vistas-1_65_11zon.webp');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    display: flex;
    align-items: center;
    padding: 4rem 2rem;
    overflow: hidden;
  }

  /* Overlays y efectos de fondo */
  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      rgba(13, 32, 95, 0.85) 0%,
      rgba(0, 0, 0, 0.75) 100%
    );
    z-index: 1;
  }

  .hero-pattern {
    position: absolute;
    inset: 0;
    background-image: 
      repeating-linear-gradient(
        45deg,
        transparent,
        transparent 100px,
        rgba(255, 255, 255, 0.02) 100px,
        rgba(255, 255, 255, 0.02) 200px
      );
    z-index: 2;
  }

  /* Elementos flotantes decorativos */
  .floating-element {
    position: absolute;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.1), transparent);
    animation: float 20s infinite ease-in-out;
    z-index: 2;
  }

  .element-1 {
    width: 300px;
    height: 300px;
    top: 10%;
    left: 5%;
    animation-delay: 0s;
  }

  .element-2 {
    width: 200px;
    height: 200px;
    top: 60%;
    right: 10%;
    animation-delay: -7s;
  }

  .element-3 {
    width: 150px;
    height: 150px;
    bottom: 20%;
    left: 15%;
    animation-delay: -14s;
  }

  @keyframes float {
    0%, 100% {
      transform: translate(0, 0) scale(1);
      opacity: 0.3;
    }
    50% {
      transform: translate(30px, -30px) scale(1.1);
      opacity: 0.5;
    }
  }

  /* Container */
  .hero-container {
    position: relative;
    z-index: 3;
    width: 100%;
    max-width: 1600px;
    margin: 0 auto;
    display: flex;
    gap: 4rem;
    align-items: flex-start;
  }

  /* Social Sidebar - FIJO EN WEB */
  .social-sidebar {
    position: sticky;
    top: 50%;
    transform: translateY(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
    padding: 2rem 0;
    flex-shrink: 0;
  }

  .social-line {
    width: 2px;
    height: 60px;
    background: linear-gradient(
      to bottom,
      transparent,
      rgba(255, 255, 255, 0.3),
      transparent
    );
  }

  .social-link {
    position: relative;
    color: white;
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    opacity: 0.8;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .social-link:hover {
    opacity: 1;
    transform: translateX(8px) scale(1.1);
    background: rgba(255, 255, 255, 0.15);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
  }

  .social-link[data-social="facebook"]:hover {
    border-color: #1877f2;
    box-shadow: 0 8px 25px rgba(24, 119, 242, 0.4);
  }

  .social-link[data-social="instagram"]:hover {
    border-color: #e4405f;
    box-shadow: 0 8px 25px rgba(228, 64, 95, 0.4);
  }

  .social-link[data-social="tiktok"]:hover {
    border-color: #ffffff;
    box-shadow: 0 8px 25px rgba(255, 255, 255, 0.3);
  }

  .social-link[data-social="tripadvisor"]:hover {
    border-color: #00af87;
    box-shadow: 0 8px 25px rgba(0, 175, 135, 0.4);
  }

  .social-link[data-social="youtube"]:hover {
    border-color: #ff0000;
    box-shadow: 0 8px 25px rgba(255, 0, 0, 0.4);
  }

  .social-tooltip {
    position: absolute;
    right: 70px;
    background: rgba(0, 0, 0, 0.9);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 6px;
    font-size: 0.85rem;
    font-weight: 600;
    white-space: nowrap;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
  }

  .social-tooltip::before {
    content: '';
    position: absolute;
    left: -6px;
    top: 50%;
    transform: translateY(-50%);
    border: 6px solid transparent;
    border-right-color: rgba(0, 0, 0, 0.9);
  }

  .social-link:hover .social-tooltip {
    opacity: 1;
  }

  /* Main Content */
  .main-content {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  /* Announcement Banner */
  .announcement-banner {
    position: relative;
    width: 100%;
    background: linear-gradient(135deg, rgba(203, 153, 52, 0.95) 0%, rgba(166, 123, 45, 0.95) 100%);
    backdrop-filter: blur(15px);
    overflow: hidden;
    box-shadow: 0 8px 32px rgba(203, 153, 52, 0.3), inset 0 1px 0 rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    border: 1px solid rgba(255, 215, 0, 0.3);
    margin-bottom: 2rem;
    animation: fadeInDown 0.8s ease-out;
  }

  .announcement-banner::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 200%;
    height: 100%;
    background: repeating-linear-gradient(
      45deg,
      transparent,
      transparent 20px,
      rgba(255, 255, 255, 0.05) 20px,
      rgba(255, 255, 255, 0.05) 40px
    );
    animation: movePattern 25s linear infinite;
    z-index: 1;
  }

  @keyframes movePattern {
    0% { transform: translateX(0); }
    100% { transform: translateX(50%); }
  }

  .announcement-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    padding: 1.2rem 2rem;
    position: relative;
    z-index: 2;
  }

  .announcement-icon {
    width: 48px;
    height: 48px;
    background: rgba(0, 43, 79, 0.2);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    animation: iconPulse 2s ease-in-out infinite;
    border: 2px solid rgba(0, 43, 79, 0.3);
  }

  .announcement-icon svg {
    color: #002b4f;
    filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.2));
  }

  @keyframes iconPulse {
    0%, 100% {
      transform: scale(1);
      box-shadow: 0 0 0 0 rgba(0, 43, 79, 0.4);
    }
    50% {
      transform: scale(1.05);
      box-shadow: 0 0 0 10px rgba(0, 43, 79, 0);
    }
  }

  .announcement-text {
    flex: 1;
    display: flex;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .urgent-badge {
    display: inline-flex;
    background: #002b4f;
    color: #ffd700;
    padding: 0.4rem 1rem;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 1px;
    animation: badgePulse 1.5s ease-in-out infinite;
    box-shadow: 0 4px 12px rgba(0, 43, 79, 0.4);
  }

  @keyframes badgePulse {
    0%, 100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.9;
      transform: scale(0.98);
    }
  }

  .message {
    color: white;
    font-size: 1rem;
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.2);
  }

  .message strong {
    font-weight: 800;
    color: #002b4f;
  }

  .spots-remaining {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    background: rgba(0, 43, 79, 0.25);
    padding: 0.3rem 0.8rem;
    border-radius: 12px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(0, 43, 79, 0.3);
  }

  .number {
    font-size: 1.3rem;
    font-weight: 900;
    color: #002b4f;
    animation: numberPulse 2s ease-in-out infinite;
  }

  @keyframes numberPulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.15); }
  }

  .spots-text {
    font-size: 0.85rem;
    font-weight: 600;
    color: white;
  }

  .announcement-actions {
    display: flex;
    gap: 1rem;
    flex-shrink: 0;
  }

  .reserve-button {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    background: #002b4f;
    color: #ffd700;
    padding: 0.8rem 2rem;
    border-radius: 50px;
    font-weight: 800;
    font-size: 0.95rem;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0, 43, 79, 0.4);
    border: 2px solid rgba(0, 43, 79, 0.3);
  }

  .reserve-button:hover {
    background: #003d6b;
    transform: translateY(-2px) scale(1.05);
    box-shadow: 0 6px 25px rgba(0, 43, 79, 0.6);
    border-color: #002b4f;
  }

  .reserve-button svg {
    transition: transform 0.3s ease;
  }

  .reserve-button:hover svg {
    transform: translateX(4px);
  }

  .close-banner {
    width: 32px;
    height: 32px;
    background: rgba(0, 43, 79, 0.2);
    border: 1px solid rgba(0, 43, 79, 0.3);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.3s ease;
    flex-shrink: 0;
    color: #002b4f;
  }

  .close-banner:hover {
    background: rgba(0, 43, 79, 0.3);
    transform: rotate(90deg);
  }

  .progress-bar {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 3px;
    background: linear-gradient(90deg, #002b4f 0%, #005aa3 100%);
    animation: progress 30s linear infinite;
    box-shadow: 0 0 10px rgba(0, 90, 163, 0.6);
    z-index: 3;
  }

  @keyframes progress {
    0% { width: 0%; }
    100% { width: 100%; }
  }

  /* Top Badge */
  .top-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    background: rgba(255, 255, 255, 0.15);
    backdrop-filter: blur(10px);
    padding: 0.7rem 1.5rem;
    border-radius: 50px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: white;
    font-size: 0.9rem;
    font-weight: 600;
    align-self: flex-start;
    animation: fadeInDown 1s ease;
  }

  .top-badge svg {
    animation: rotate 3s linear infinite;
  }

  @keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  /* Text Content */
  .text-content {
    max-width: 1200px;
    text-align: center;
    margin: 0 auto;
    animation: fadeInUp 1s ease 0.2s backwards;
  }

  .text-content h1 {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    margin-bottom: 2rem;
  }

  .title-line {
    font-size: clamp(2.5rem, 6vw, 5rem);
    font-weight: 900;
    color: white;
    line-height: 1;
    letter-spacing: -0.03em;
    text-shadow: 0 0 20px 3px rgba(0, 0, 0, 0.5);
  }

  .title-line.highlight {
    position: relative;
    background: white;
    background-blend-mode: overlay, multiply, normal;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .title-line.highlight::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 200px;
    height: 4px;
    background: linear-gradient(90deg, transparent, #ffd700, transparent);
    border-radius: 2px;
  }

  .hero-description {
    color: rgba(255, 255, 255, 0.95);
    font-size: clamp(1rem, 1.5vw, 1.2rem);
    line-height: 1.8;
    max-width: 800px;
    margin: 0 auto 2rem;
    font-weight: 300;
  }

  /* Stats Row */
  .stats-row {
    display: flex;
    justify-content: center;
    gap: 4rem;
    flex-wrap: wrap;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 900;
    color: #cb9934;
    line-height: 1;
  }

  .stat-label {
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.9rem;
    font-weight: 500;
  }

  /* Tour Cards */
  .tour-cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    animation: fadeInUp 1s ease 0.4s backwards;
  }

  .tour-card {
    background: rgba(255, 255, 255, 0.98);
    border-radius: 20px;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    cursor: pointer;
    position: relative;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.3);
  }

  .tour-card[data-featured="true"] {
    border: 2px solid #cb9934;
  }

  .tour-card:hover {
    transform: translateY(-12px) scale(1.02);
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.5);
  }

  .featured-badge {
    position: absolute;
    top: 15px;
    right: 15px;
    background: linear-gradient(135deg, #b21f27, #d9262f);
    color: #ffffff;
    padding: 0.4rem 0.9rem;
    border-radius: 20px;
    font-size: 0.9rem;
    font-weight: 700;
    z-index: 10;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .card-image-wrapper {
    position: relative;
    height: 280px;
    overflow: hidden;
  }

  .card-image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .tour-card:hover .card-image-wrapper img {
    transform: scale(1.15) rotate(2deg);
  }

  .image-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to top,
      rgba(0, 0, 0, 0.7) 0%,
      transparent 60%
    );
  }

  .image-badges {
    position: absolute;
    bottom: 15px;
    left: 15px;
    right: 15px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    z-index: 5;
  }

  .difficulty-badge {
    background: rgba(255, 255, 255, 0.95);
    padding: 0.4rem 0.9rem;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .difficulty-badge[data-difficulty="easy"], 
  .difficulty-badge[data-difficulty="moderate"], 
  .difficulty-badge[data-difficulty="challeging"] {
    color: #d9262f;
  }

  .rating-badge {
    background: #cb9934;
    color: #fff;
    padding: 0.4rem 0.8rem;
    border-radius: 20px;
    font-size: 1rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    gap: 0.3rem;
  }

  .card-content {
    padding: 1.5rem;
  }

  .card-title {
    font-size: 1.2rem;
    font-weight: 800;
    color: #002b4f;
    margin-bottom: 0.5rem;
    line-height: 1.3;
  }

  .card-subtitle {
    color: #666;
    font-size: 0.9rem;
    margin-bottom: 1.2rem;
    line-height: 1.4;
  }

  .card-info {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 0;
    border-top: 1px solid #e8e8e8;
    border-bottom: 1px solid #e8e8e8;
    margin-bottom: 1.2rem;
  }

  .info-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: #666;
    font-size: 0.85rem;
    font-weight: 600;
  }

  .info-item svg {
    color: #002b4f;
  }

  .price-tag {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
  }

  .from-label {
    font-size: 0.7rem;
    color: #999;
    font-weight: 600;
  }

  .price-value {
    font-size: 1.4rem;
    font-weight: 900;
    color: #002b4f;
    line-height: 1;
  }

  .card-button {
    width: 100%;
    background: linear-gradient(135deg, #002b4f, #005aa3);
    color: white;
    border: none;
    padding: 0.9rem 1.5rem;
    border-radius: 10px;
    font-size: 0.9rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.7rem;
    cursor: pointer;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    text-decoration: none;
    margin-bottom: 0.8rem;
  }

  .card-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(13, 95, 95, 0.4);
  }

  .card-button-details {
    width: 100%;
    background: linear-gradient(135deg, #c00d0d, #8c1212);
    color: white;
    border: none;
    padding: 0.9rem 1.5rem;
    border-radius: 10px;
    font-size: 0.9rem;
    font-weight: 700;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.7rem;
    cursor: pointer;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    text-decoration: none;
    box-shadow: 0 4px 15px rgba(37, 211, 102, 0.3);
  }

  .card-button-details:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(211, 37, 37, 0.4);
    background: linear-gradient(135deg, #8c1212, #5e0707);
  }

  .card-shine {
    position: absolute;
    top: 0;
    left: -100%;
    width: 50%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.3),
      transparent
    );
    transition: left 0.6s ease;
    pointer-events: none;
  }

  .tour-card:hover .card-shine {
    left: 100%;
  }

  /* CTA Section */
  .cta-section {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.2rem;
    margin-top: 1rem;
    animation: fadeInUp 1s ease 0.6s backwards;
  }

  .cta-buttons {
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .cta-button {
    background: linear-gradient(135deg, #cb9934, #a67b2d);
    color: #fff;
    padding: 1.2rem 3rem;
    border-radius: 50px;
    display: flex;
    align-items: center;
    gap: 1rem;
    font-weight: 800;
    font-size: 1.1rem;
    text-decoration: none;
    transition: all 0.4s ease;
    box-shadow: 0 8px 30px rgba(255, 215, 0, 0.4);
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .cta-button:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(255, 215, 0, 0.6);
  }

  .cta-note {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.9);
    font-size: 0.9rem;
    font-weight: 500;
    justify-content: center;
    text-align: center;
  }

  .cta-note svg {
    color: #4caf50;
  }

  /* Scroll Indicator */
  .scroll-indicator {
    position: absolute;
    bottom: 3rem;
    right: 3rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.8);
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 2px;
    z-index: 10;
  }

  .scroll-line {
    width: 2px;
    height: 40px;
    background: linear-gradient(
      to bottom,
      rgba(255, 255, 255, 0.5),
      transparent
    );
    animation: scrollDown 2s infinite ease-in-out;
  }

  @keyframes scrollDown {
    0%, 100% {
      transform: translateY(0);
      opacity: 0.5;
    }
    50% {
      transform: translateY(10px);
      opacity: 1;
    }
  }

  /* Animations */
  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive */
  @media (max-width: 1400px) {
    .hero-container {
      gap: 3rem;
    }
  }

  @media (max-width: 1200px) {
    .tour-cards {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 1024px) {
    .stats-row {
      gap: 2rem;
    }

    .tour-cards {
      gap: 1.5rem;
    }

    .announcement-content {
      padding: 1rem 1.5rem;
      gap: 1rem;
    }

    .announcement-icon {
      width: 40px;
      height: 40px;
    }

    .message {
      font-size: 0.9rem;
    }

    .reserve-button {
      padding: 0.7rem 1.5rem;
      font-size: 0.85rem;
    }
  }

  @media (max-width: 768px) {
    .hero-section {
      padding: 3rem 1.5rem;
      background-attachment: scroll;
    }

    .hero-container {
      flex-direction: column;
      gap: 2rem;
    }

    /* Announcement Banner Mobile */
    .announcement-banner {
      margin-bottom: 1.5rem;
    }

    .announcement-content {
      flex-direction: column;
      text-align: center;
      padding: 1.2rem 1rem;
      gap: 1rem;
    }

    .announcement-text {
      flex-direction: column;
      gap: 0.8rem;
      text-align: center;
    }

    .message {
      flex-direction: column;
      text-align: center;
    }

    .announcement-actions {
      width: 100%;
    }

    .reserve-button {
      width: 100%;
      justify-content: center;
    }

    .close-banner {
      position: absolute;
      top: 10px;
      right: 10px;
    }

    /* Redes sociales en móvil - horizontal */
    .social-sidebar {
      position: static;
      transform: none;
      flex-direction: row;
      justify-content: center;
      padding: 0;
      order: -1;
    }

    .social-line {
      width: 60px;
      height: 2px;
      background: linear-gradient(
        to right,
        transparent,
        rgba(255, 255, 255, 0.3),
        transparent
      );
    }

    .social-link:hover {
      transform: translateY(-8px) scale(1.1);
    }

    .social-tooltip {
      left: 50%;
      top: -50px;
      transform: translateX(-50%);
    }

    .social-tooltip::before {
      left: 50%;
      top: 100%;
      transform: translateX(-50%);
      border: 6px solid transparent;
      border-top-color: rgba(0, 0, 0, 0.9);
    }

    .tour-cards {
      grid-template-columns: 1fr;
    }

    .title-line {
      font-size: clamp(2rem, 8vw, 3rem);
    }

    .stats-row {
      gap: 1.5rem;
    }

    .stat-number {
      font-size: 2rem;
    }

    .scroll-indicator {
      display: none;
    }
  }

  @media (max-width: 480px) {
    .hero-section {
      padding: 2rem 1rem;
    }

    .top-badge {
      font-size: 0.75rem;
      padding: 0.5rem 1rem;
    }

    .hero-description {
      font-size: 0.95rem;
    }

    .card-image-wrapper {
      height: 220px;
    }

    .cta-button {
      padding: 1rem 2rem;
      font-size: 0.95rem;
    }

    /* Announcement Banner Small Mobile */
    .announcement-content {
      padding: 1rem;
    }

    .announcement-icon {
      width: 36px;
      height: 36px;
    }

    .message {
      font-size: 0.85rem;
    }

    .urgent-badge {
      font-size: 0.7rem;
      padding: 0.3rem 0.8rem;
    }

    .number {
      font-size: 1.1rem;
    }

    .spots-text {
      font-size: 0.75rem;
    }
  }
</style>